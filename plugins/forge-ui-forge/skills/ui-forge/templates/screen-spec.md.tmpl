# {{SCREEN_TITLE}}

> Screen ID: `{{SCREEN_ID}}`

## Intent

{{INTENT_ONE_OR_TWO_SENTENCES}}

## Structure

```
{{SEMANTIC_TREE}}
```

<!-- Top-down semantic tree. Name the regions, not the classes. -->

## Composition

Registry components used:
{{REGISTRY_COMPONENTS_LIST}}

<!-- Example:
- `kpi-card` v2 — four instances in the top row.
- `data-table-dense` v1 — main holdings table.
-->

New components introduced in this screen (promoted in Phase 4):
{{NEW_COMPONENTS_LIST}}

Screen-local blocks (NOT promoted — one-offs):
{{SCREEN_LOCAL_LIST}}

Fixtures referenced:
{{FIXTURES_LIST}}

## Design tokens used

- **Colors:** {{COLORS_USED}}
- **Spacing:** {{SPACING_USED}}
- **Typography:** {{TYPOGRAPHY_USED}}
- **Radii:** {{RADII_USED}}
- **Shadows:** {{SHADOWS_USED}}

## States

<!-- Screen-level states. Component-level states live in the per-component specs. -->

- **happy**: {{HAPPY_STATE}}
- **empty**: {{EMPTY_STATE}}
- **loading**: {{LOADING_STATE}}
- **error**: {{ERROR_STATE}}
- **edge (high density / long names / etc.)**: {{EDGE_STATE}}

## Interactions

{{INTERACTIONS}}

<!-- Screen-level orchestration. Filter changes that refetch, modal flows, navigation transitions. -->

## Behavior

<!--
  Behavioral spec — the WHAT-it-DOES, complementary to the visual States section above.
  Populated in Phase 4 from:
   - The brief and (optional) Phase 1.5 `data/behavior.md` skeleton
   - Pin distillation: `state-transition` pins → § State transitions; `logic-rule` pins → § Business rules + § Validations
   - The mutation-related fields of `data/scenarios/*.json` (loading/error variants imply transitions)

  Heuristic for splitting logic-rule pins:
   - Comment starts with "validación:" / "validation:" OR mentions a specific named field → § Validations
   - Otherwise → § Business rules

  Subsection any of these that aren't applicable — drop the empty ones, don't leave placeholders.
-->

### State transitions

{{STATE_TRANSITIONS}}

<!-- Format: `from → to (on <trigger>, after <timing/condition>)`.
     Example:
       - `loading → loaded (on data.fetched)`
       - `loaded → error (on data.fetchFailed)`
       - `idle → submitting (on form.submit)`
       - `submitting → success (on api.200)` · navigates to `/orders/{id}`
       - `submitting → error (on api.4xx|5xx)` · keeps form, shows inline message
     Mermaid stateDiagram-v2 is acceptable when transitions are non-linear.
-->

### Business rules

{{BUSINESS_RULES}}

<!-- Numbered list of invariants the UI must respect.
     Example:
       1. Selling requires an open position in the same ticker.
       2. Single-order amount over 10000 EUR triggers 2FA confirmation.
       3. Markets are read-only outside trading hours (09:00–17:30 CET).
-->

### Validations

{{VALIDATIONS}}

<!-- Per-field constraints. Format: `field: rules (separated by commas)`.
     Example:
       - amount: required, positive, ≤ portfolioBalance
       - ticker: required, must exist in fixtures/tickers.json
       - notes: optional, ≤ 280 chars
-->

### Mutation contracts

{{MUTATION_CONTRACTS}}

<!-- For each user-driven mutation:
       <action-name>:
         preconditions: <what must be true>
         effects: <what happens on the server / local state>
         success path: <UI consequence on 2xx>
         error path: <UI consequence on 4xx/5xx>
     Example:
       submitOrder:
         preconditions: form.valid, balance ≥ amount
         effects: POST /orders {ticker, amount}
         success: navigate to /orders/{id}, toast "Order placed"
         error: inline message on form, no navigation
-->

### Conditional rendering

{{CONDITIONAL_RENDERING}}

<!-- Rules beyond the static scenarios in States. Format: `if <condition> → <render>`.
     Example:
       - if transactions.length > 100 → render pagination (page size 50)
       - if portfolio.flagged → render compliance banner above header
       - if user.role !== "trader" → hide quick-trade panel
-->

## Data contract

```json
{{DATA_CONTRACT_JSON}}
```

References: `screens/{{SCREEN_ID}}/data/schema.json`

## Accessibility

- **Landmark roles:** {{LANDMARK_ROLES}}
- **Tab order:** {{TAB_ORDER}}
- **Focus management:** {{FOCUS_MANAGEMENT}}
- **Keyboard shortcuts:** {{KEYBOARD_SHORTCUTS}}

## Responsive

| Breakpoint | Layout behavior |
|------------|-----------------|
| `< 640px` (sm) | {{SM_LAYOUT}} |
| `≥ 768px` (md) | {{MD_LAYOUT}} |
| `≥ 1024px` (lg) | {{LG_LAYOUT}} |
| `≥ 1536px` (2xl) | {{XXL_LAYOUT}} |

## Out of scope

{{OUT_OF_SCOPE}}

<!-- Examples:
- Analytics events (tracked separately).
- Auth gating (handled by the route layer).
- i18n strings (will be keyed in a later pass).
-->
